apiVersion: v1
kind: Service
metadata:
  name: clinic-api
  labels:
    app: clinic-api
    domain: health
spec:
  type: ClusterIP
  selector:
    app: clinic-api
  ports:
    - name: http
      port: 80
      targetPort: http
