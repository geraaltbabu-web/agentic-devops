# Labs

Validate `app.yaml` mentally: which keys are strings vs bools? Run `python parse_yaml_demo.py` if PyYAML is installed; otherwise read the comments.

```bash
python parse_yaml_demo.py   # optional
```
