# Stack

GitHub Actions, GitLab CI, K8s, Helm, Ansible, Argo, Prometheus rules, CloudFormation, docker-compose: all YAML/JSON. Python/Go generate them. Terraform is HCL (next-next modules) but still emits JSON.
