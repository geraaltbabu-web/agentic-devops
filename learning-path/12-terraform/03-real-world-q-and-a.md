# Q&A

**Plan shows destroy in prod?** Stop. Explain each destroy. Maybe wrong workspace.

**Secrets in .tfvars committed?** Rotate. Use secret stores.

**Two applies on same state?** Lock. If stuck, verify no other job then unlock with protocol.
