# Language

Resource graph. Implicit deps vs `depends_on`. Count/for_each. Sensitive variables. Provider version pins.

Modules: versioned, not `../hack`. Workspaces are not a substitute for separate states per blast radius.
