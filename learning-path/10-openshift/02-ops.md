# Operations

`oc login`, `oc whoami`, `oc new-project`, `oc apply -f`, `oc rollout status`, `oc logs`, `oc adm policy`.

Image streams vs external registries. Pull secrets.

Quotas and limit ranges per project.

Upgrades: ROSA vs self-managed—who owns etcd backups, version skew, worker pools.

GitOps: OpenShift GitOps (Argo CD). Application in `openshift-gitops` (see `banking-platform/gitops`).
