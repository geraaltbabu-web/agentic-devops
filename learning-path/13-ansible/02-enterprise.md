# Enterprise

`--check --diff` in PR against a lab host. Vault or external secret lookup. Don’t put prod passwords in group_vars in Git.

AWX/AAP: RBAC, schedules, credentials store, audit.

Immutable images reduce Ansible on prod boxes; still used for jump hosts, appliances, and brownfield.
