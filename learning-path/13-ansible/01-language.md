# Language

YAML plays, `hosts:`, `become:`, modules (`yum`, `copy`, `systemd`). Idempotent: running twice is a no-op.

Inventory: static ini/yaml or dynamic (AWS). Groups for `prod` vs `dev`.

Roles: Galaxy structure. Pin versions.
