# Q&A

**Why did it change every run?** Not idempotent; template always writes; command module instead of module.

**Raw SSH vs Ansible?** Ansible gives logging, retries, modules.

**Windows?** WinRM; different beast. This path focuses Linux.
