# Q&A

**K8s Secret encrypted?** etcd encryption at rest is extra; default is not enough.

**OIDC sub too wide?** `repo:org/*` is a footgun.

**Vault token in Jenkins global?** Use approle/JWT per job.
