# Golden path

Template repo: app + Dockerfile + Helm + CI + dashboards. `cookiecutter` or Backstage software templates.

Self-service: namespace/project via PR to GitOps, not a ticket that waits 5 days—*if* quotas and network policies apply automatically.
