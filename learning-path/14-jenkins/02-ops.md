# Ops

Backup JENKINS_HOME or use CasC (JCasC). Plugins are a supply chain; pin and test upgrades.

Controller HA is hard; treat it as critical. Agents ephemeral.

OIDC/cloud credentials same as GitHub Actions (module 01).
