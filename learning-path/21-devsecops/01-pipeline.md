# Pipeline controls

PR: secret scan fail-closed. High CVE in **your** code vs base image (different SLAs). License policy. IaC misconfig.

SBOM attached to digest. Sign the image. CI verifies signature before deploy.
