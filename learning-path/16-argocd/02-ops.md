# Ops

SSO into Argo. Fine-grained projects. Repo credentials. Cluster secrets.

Notifications to chat. Sync windows. Ignore differences for generated fields.

Disaster: Git still has desired; recover cluster then re-sync. Backup Argo redis/repo-server is secondary to Git.
