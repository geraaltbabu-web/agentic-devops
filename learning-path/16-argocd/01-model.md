# Model

```text
Git SHA -> Argo Application -> live objects
```

Drift: someone clicked in console. SelfHeal resets to Git (good or surprising—protect prod).

Progressive syncs, waves, hooks. Don’t auto-sync prod until you have metrics hooks or manual.

App-of-apps or ApplicationSet for many clusters.
