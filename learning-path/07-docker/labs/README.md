# Labs

Read `Dockerfile` then (if Docker is installed):

```bash
docker build -t mastery-hello:lab .
docker run --rm -p 18081:8080 mastery-hello:lab
```

If Docker is missing, still explain every line of the Dockerfile (required).
