# Prometheus

Scrape `/metrics`. Labels: low cardinality. `rate()`, `histogram_quantile`. Recording rules for expensive queries.

Alertmanager: routes, inhibit, silence. Page on symptoms (5xx, latency) not CPU alone.
