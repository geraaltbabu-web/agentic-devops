# Labs

```bash
cd learning-path/04-python/labs
python health.py --url https://example.com --json
python -m pytest test_health.py -q
```

On Windows use the same commands from the repo root with `python`.
