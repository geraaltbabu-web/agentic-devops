# Capstone

Ship a `python -m` tool that: reads a JSON policy file, validates image name allowlist (copy the idea from `banking-platform/agent`), prints a plan, exits 2 on policy fail. Tests without network. No credentials in Git.
