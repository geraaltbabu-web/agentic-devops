# Python in the stack

Glue for Terraform wrappers, AWS inventory, Kubernetes wait loops, Jenkins API, GitLab pipelines, log extractors, OPA/conftest helpers, and chatops. The banking `agent/deploy_agent.py` is the in-repo example: policy, plan JSON, optional LLM, no secrets in Git.
